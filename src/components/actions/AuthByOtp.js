import {
    LOGIN_SUCCESS,
    LOGIN_FAIL,
    LOGOUT,
    SET_MESSAGE,
  } from "./types";

  import AuthServicesByOtp from "../services/auth.services.otp";

  const loginBYOtp=(email='',otp='')=>async (dispatch)=>{

    // try {
    //     const res=await 
    // } catch (error) {
        
    // }

  }